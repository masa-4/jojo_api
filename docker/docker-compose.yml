version: '3'
services:
    api_server:
        build:
            context: .
            dockerfile: ./server/DockerFile
        command: /bin/sh -c "go run main.go"
        stdin_open: true
        tty: true
        volumes: 
            - ../src:/app
        ports:
            - 8080:8080